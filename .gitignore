.env
.env.*
!.env.example
# Real deployment target (host/IP, SSH key path, remote paths). Keep the
# committed deploy/deploy.env.example as the template.
deploy/deploy.env
data/
backups/
dist/
*.db
*.db-journal
*.db-wal
*.db-shm
node_modules/
.svelte-kit/
build/
dist/
.DS_Store
.claude/
Fund dashboard frontend design/
# Local design decisions and browser verification captures.
.impeccable/direction.json
.impeccable/questions/
.impeccable/review/
